# predperf--same loglik behavior for insample, k and test

# TODO: pred measure print should have a tag for where the data is from
# insample won't be able to estimate effective # of params, but k-fold and test can (need in sample loglik to compare against OOS). That should be handled in print
# k-fold should store the splits
