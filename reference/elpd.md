# Expected log-predictive density

Expected log-predictive density

## Usage

``` r
elpd(ylp, log_weights)
```

## Arguments

- ylp:

  matrix of posterior draws (S x n) of pointwise LOO log predictive
  densities

- log_weights:

  matrix of standardized loo weights (S x n) on the log scale
