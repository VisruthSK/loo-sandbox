# Mean log-predictive density

Mean log-predictive density

## Usage

``` r
mlpd(ylp, log_weights)
```

## Arguments

- ylp:

  matrix of posterior draws (S x n) of pointwise log predictive
  densities

- log_weights:

  matrix of standardized loo weights (S x n) on the log scale
